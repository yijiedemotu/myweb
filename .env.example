# 后台管理密码（必改！）
#
# 这个值在不同场景下的来源不一样，别搞混：
#
#   本地 next dev        → 放在 .env.local（Next 自己加载）
#   本地 npm run preview → 放在 .dev.vars（wrangler dev 读这个）
#   线上 Workers         → 用 `npx wrangler secret put ADMIN_PASSWORD` 设置，
#                          不要写进 wrangler.jsonc 的 vars（那是明文）
ADMIN_PASSWORD=changeme

# 站点完整地址：RSS 生成绝对链接用。留空时会根据请求的 Host 头推断。
#
# 建议留空 —— 绑好自定义域名后，Host 兜底会自动产出正确的绝对链接。
# 若要固定：注意 NEXT_PUBLIC_* 是「构建期」内联进产物的，改运行时变量不生效，
# 必须放在构建变量里（Worker → Settings → Build → Variables and Secrets）并重新部署，
# 然后访问 /rss.xml 确认 <link> 已生效。
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
